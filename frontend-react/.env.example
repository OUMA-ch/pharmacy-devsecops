# URL de base de l'API backend Spring Boot (sans slash final).
# En local avec docker-compose : http://localhost:8080
# En production : l'URL du backend deploye (ex: Render).
VITE_API_BASE_URL=http://localhost:8080